---For cors----
if i want to access localhost3000, then 1st do this "origin:  "http://localhost:3000" || process.env.CLIENT_URL," and active local host push this and again closde restart every thing

if want the render then again set "origin: process.env.CLIENT_URL || "http://localhost:3000"," and active server via thunder client api endpoint. and push


 npm i express axios express-validator @cloudinary/react @cloudinary/url-gen bcrypt cloudinary cors dotenv jsonwebtoken mongoose nodemon